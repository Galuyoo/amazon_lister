# Archive

This folder contains historical project notes that are not part of the main Streamlit runtime.

The production app entrypoint is app.py.

Current runtime folders: config/, templates/, services/, utils/.
